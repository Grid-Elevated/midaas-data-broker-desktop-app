name: Build and Release Dev

on:
  push:
    branches:
      - dev

jobs:
  bump-version:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    outputs:
      new_version: ${{ steps.bump.outputs.new_version }}
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Bump patch version in dev config
        id: bump
        run: |
          CURRENT=$(jq -r '.version' desktop/src-tauri/tauri.conf.dev.json)
          IFS='.' read -r major minor patch <<< "$CURRENT"
          NEW="$major.$minor.$((patch + 1))"
          echo "new_version=$NEW" >> $GITHUB_OUTPUT

          jq ".version = \"$NEW\"" desktop/src-tauri/tauri.conf.dev.json > tmp.json && mv tmp.json desktop/src-tauri/tauri.conf.dev.json

          echo "Bumped $CURRENT → $NEW"

      - name: Commit version bump
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add desktop/src-tauri/tauri.conf.dev.json
          git commit -m "chore: bump dev version to ${{ steps.bump.outputs.new_version }} [skip ci]"
          git push

  build:
    needs: bump-version
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: macos-latest
            args: --target universal-apple-darwin
          - platform: ubuntu-22.04
            args: ''
          - platform: windows-latest
            args: ''

    runs-on: ${{ matrix.platform }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: dev

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.platform == 'macos-latest' && 'aarch64-apple-darwin,x86_64-apple-darwin' || '' }}

      - name: Install Linux dependencies
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf

      - name: Install frontend dependencies
        working-directory: desktop
        run: npm install

      - uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          VITE_APP_VARIANT: beta
        with:
          projectPath: desktop
          tagName: dev-v${{ needs.bump-version.outputs.new_version }}
          releaseName: Dev v${{ needs.bump-version.outputs.new_version }}
          releaseBody: "Development build — not for production use."
          releaseDraft: false
          prerelease: true
          args: ${{ matrix.args }} --config src-tauri/tauri.conf.dev.json

  notify:
    needs: [bump-version, build]
    runs-on: ubuntu-latest
    steps:
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-1

      - name: Send beta release email via SES
        run: |
          VERSION="dev-v${{ needs.bump-version.outputs.new_version }}"
          RELEASE_URL="https://github.com/Grid-Elevated/midaas-data-broker-desktop-app/releases/tag/${VERSION}"
          DOWNLOAD_URL="https://dev.midaas.ai/dataset-manager"

          aws ses send-email \
            --region us-east-1 \
            --from "noreply@midaas.ai" \
            --destination "ToAddresses=russel.brouillard@gridelevated.com" \
            --message "{
              \"Subject\": {\"Data\": \"MIDAAS Data Broker Beta ${VERSION} Ready for Testing\", \"Charset\": \"UTF-8\"},
              \"Body\": {
                \"Html\": {
                  \"Charset\": \"UTF-8\",
                  \"Data\": \"<p>A new beta build of the MIDAAS Data Broker is ready for internal testing.</p><p><strong>Version:</strong> ${VERSION}</p><p>Download the beta installer from the dev Dataset Manager page:</p><p><a href='${DOWNLOAD_URL}'>${DOWNLOAD_URL}</a></p><p>Release details: <a href='${RELEASE_URL}'>${RELEASE_URL}</a></p><p><em>This is a beta build — please report any issues before it is promoted to production.</em></p><br><p>MIDAAS Team</p>\"
                },
                \"Text\": {
                  \"Charset\": \"UTF-8\",
                  \"Data\": \"MIDAAS Data Broker Beta ${VERSION} is ready for internal testing.\n\nDownload (dev): ${DOWNLOAD_URL}\nRelease: ${RELEASE_URL}\n\nThis is a beta build — please report any issues before it is promoted to production.\"
                }
              }
            }"
